// src/components/Header.js

import React from 'react';

const Header = () => (
  <header className="mb-5">
    <h1 className="font-bold text-4xl uppercase tracking-wide">Invoice</h1>
  </header>
);

export default Header;
